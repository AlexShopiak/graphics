'use strict'

class Compiler {
    #ast

    compile(expr) { //todo
    //expr to Abstract syntax tree       
    }

    compute(value) { //todo
        //find ast from value
        return 1;
    }
}

module.exports = {Compiler}